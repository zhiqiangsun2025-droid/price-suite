name: Build Windows EXE

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        cd client
        pip install customtkinter requests pillow cryptography pandas openpyxl pyperclip
        pip install pyinstaller pyautogui pywinauto opencv-python jieba scikit-learn
    
    - name: Build EXE
      run: |
        cd client
        pyinstaller --onefile --windowed --name="智能选品系统" --hidden-import=customtkinter --hidden-import=PIL --hidden-import=pandas --hidden-import=openpyxl --hidden-import=pyautogui --hidden-import=pywinauto --hidden-import=cv2 --hidden-import=requests --hidden-import=cryptography --hidden-import=jieba --hidden-import=sklearn --clean modern_client.py
    
    - name: Upload EXE
      uses: actions/upload-artifact@v3
      with:
        name: 智能选品系统
        path: client/dist/*.exe
        retention-days: 30
    
    - name: Display build info
      run: |
        cd client/dist
        dir



