# 🎯 宝塔面板部署完整指南

## 版本信息
- **项目**: 智能选品系统（Price Suite）
- **版本**: 20251024001
- **压缩包**: price-suite-full-20251024.tar.gz（252KB）
- **解压后**: 约1.4MB
- **安装依赖后**: 约500MB

---

## 📦 第一步：上传压缩包

### 方法A：宝塔面板直接上传（推荐）

1. **打开宝塔面板**
   ```
   浏览器访问: http://你的服务器IP:8888
   输入用户名和密码登录
   ```

2. **进入文件管理**
   ```
   左侧菜单 → 文件
   进入目录: /opt 或 /www/wwwroot
   ```

3. **上传压缩包**
   ```
   点击顶部"上传"按钮
   选择: price-suite-full-20251024.tar.gz
   等待上传（252KB，几秒钟完成）
   ```

### 方法B：SCP上传（备选）

```bash
# Windows PowerShell 或 WSL
scp price-suite-full-20251024.tar.gz root@服务器IP:/opt/
```

---

## 📂 第二步：解压缩

1. **在宝塔面板中找到压缩包**
   ```
   文件 → /opt → price-suite-full-20251024.tar.gz
   ```

2. **右键解压**
   ```
   右键点击压缩包
   → 解压
   → 解压到当前目录
   → 确认
   ```

3. **验证解压结果**
   ```
   解压后目录结构:
   /opt/
     └── .github/
     └── client/
     └── server/
     └── rpa/
     └── docs/
     └── scripts/
     └── README.md
     └── .gitignore
   ```

---

## 🐍 第三步：配置Python环境（宝塔）

### 1. 安装Python管理器

```
宝塔面板操作:
软件商店 → 搜索"Python项目管理器" → 安装
```

### 2. 创建Python项目

```
左侧菜单 → Python项目管理器 → 添加项目

项目配置:
  项目名称: price-suite
  项目路径: /opt  （选择你解压的目录）
  Python版本: 3.8+ （推荐3.9或3.10）
  启动方式: gunicorn
  启动文件: server/app.py
  端口: 5000
  虚拟环境: 自动创建
```

### 3. 安装服务器端依赖

```bash
方法1: 宝塔面板图形界面
  Python项目管理器 → 找到price-suite → 
  → "终端" → 输入:
  pip install -r server/requirements.txt

方法2: SSH命令行
  cd /opt
  python3 -m venv venv
  source venv/bin/activate
  pip install -r server/requirements.txt
```

**依赖列表** (server/requirements.txt):
```
flask==2.3.0
flask-cors==4.0.0
requests==2.31.0
pandas==2.0.3
openpyxl==3.1.2
selenium==4.12.0
python-dotenv==1.0.0
cryptography==41.0.3
```

---

## 🌐 第四步：配置Flask应用

### 1. 配置服务器环境变量

**创建配置文件**: `/opt/server/.env`

```bash
# 在宝塔面板"文件"中创建
路径: /opt/server/.env
内容:
FLASK_ENV=production
FLASK_APP=app.py
SERVER_HOST=0.0.0.0
SERVER_PORT=5000
SECRET_KEY=你的随机密钥
```

### 2. 初始化数据库

```bash
# 宝塔SSH终端或Python项目管理器终端
cd /opt/server
python3 admin_tool.py init
```

### 3. 启动服务器

**方法1: 宝塔Python项目管理器（推荐）**
```
Python项目管理器 → price-suite → 启动
自动守护进程，崩溃自动重启
```

**方法2: 手动启动**
```bash
cd /opt/server
source ../venv/bin/activate
python app.py
```

### 4. 配置反向代理（可选但推荐）

```
宝塔面板操作:
网站 → 添加站点
  域名: price-suite.你的域名.com
  根目录: /opt/server
  
添加完成后 → 设置 → 反向代理 → 添加
  代理名称: Flask
  目标URL: http://127.0.0.1:5000
  发送域名: $host
```

---

## 🔐 第五步：配置安全和防火墙

### 1. 放行端口

```
宝塔面板:
安全 → 放行端口 → 添加
  端口: 5000
  协议: TCP
  备注: Flask服务器
```

### 2. 配置SSL（可选）

```
网站 → 你的站点 → SSL → Let's Encrypt
自动申请免费证书
开启强制HTTPS
```

---

## 💻 第六步：Cursor远程开发配置

### 1. 配置SSH密钥登录（推荐）

```bash
# 本地WSL生成密钥
ssh-keygen -t rsa -b 4096

# 复制公钥到服务器
ssh-copy-id root@服务器IP

# 测试免密登录
ssh root@服务器IP
```

### 2. Cursor SSH配置

**Windows本地配置**: `C:\Users\你的用户名\.ssh\config`

```
Host price-suite-server
    HostName 你的服务器IP
    User root
    Port 22
    IdentityFile ~/.ssh/id_rsa
```

### 3. Cursor连接服务器

```
1. Ctrl+Shift+P → "Remote-SSH: Connect to Host"
2. 选择: price-suite-server
3. 新窗口打开: /opt
4. 开始远程开发！
```

---

## 🧪 第七步：测试验证

### 1. 测试服务器API

```bash
# 本地测试
curl http://服务器IP:5000/health

# 或浏览器访问
http://服务器IP:5000/health
```

**预期返回**:
```json
{"status": "ok", "version": "20251024001"}
```

### 2. 测试客户端连接

```bash
# 本地WSL，修改客户端配置
cd /home/user/projects/shopxo-master/apps/price-suite/client
nano config_client.json

# 修改服务器地址
{
  "server_url": "http://服务器IP:5000"
}

# 运行测试
python modern_client_ultimate.py
```

---

## 📊 宝塔面板日常管理

### 查看日志

```
Python项目管理器 → price-suite → 日志
或
文件 → /opt/server/app.log
```

### 重启服务

```
Python项目管理器 → price-suite → 重启
```

### 监控性能

```
宝塔面板首页 → 系统状态
CPU/内存/磁盘/网络实时监控
```

### 定时任务（备份）

```
计划任务 → 添加任务
  任务类型: Shell脚本
  任务名称: 备份数据库
  执行周期: 每天 03:00
  脚本内容:
    cd /opt/server
    cp authorization.db /backup/auth_$(date +%Y%m%d).db
```

---

## 🎯 完整操作流程总结

```
1. 上传 price-suite-full-20251024.tar.gz 到宝塔 ✅
   ↓
2. 宝塔面板右键解压 ✅
   ↓
3. 软件商店安装"Python项目管理器" ✅
   ↓
4. 创建Python项目 + 安装依赖 ✅
   ↓
5. 启动Flask服务器（端口5000） ✅
   ↓
6. 配置防火墙放行5000端口 ✅
   ↓
7. Cursor SSH连接远程开发 ✅
   ↓
8. 开始开发！ 🎊
```

---

## ❓ 常见问题

### Q1: 依赖安装很慢？

```bash
# 使用清华源加速
pip install -r server/requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q2: 端口5000被占用？

```bash
# 查看占用
netstat -tulnp | grep 5000

# 修改端口
宝塔Python项目管理器 → 修改端口为5001
或修改 server/.env 中的 SERVER_PORT
```

### Q3: 客户端无法连接服务器？

```bash
# 检查清单
1. 服务器是否启动: 宝塔面板查看项目状态
2. 防火墙是否放行: 安全 → 检查5000端口
3. 服务器IP是否正确: ping 服务器IP
4. 客户端配置是否正确: 检查config_client.json
```

### Q4: Selenium无法启动浏览器？

```bash
# 安装Chrome
yum install -y google-chrome-stable

# 安装ChromeDriver
wget https://chromedriver.storage.googleapis.com/114.0.5735.90/chromedriver_linux64.zip
unzip chromedriver_linux64.zip
mv chromedriver /usr/bin/
chmod +x /usr/bin/chromedriver
```

---

## 🎊 部署完成！

**现在你可以**:
- ✅ 远程Cursor开发前后端
- ✅ 宝塔面板管理服务器
- ✅ 实时查看日志和性能
- ✅ 一键重启和备份
- ✅ 客户端连接云端API

**宝塔面板让部署变得超级简单！** 🚀
